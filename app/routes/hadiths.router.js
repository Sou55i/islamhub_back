import express from "express";

import {hadithController, hadithController as hadith} from "../controllers/index.controller.js";

const router = express.Router();

//router.get("/hadiths", hadithController);